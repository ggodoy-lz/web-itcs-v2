# Copiá estos valores en Vercel → Project → Settings → Environment Variables (Production + Preview).

ADMIN_PASSWORD=poné_una_clave_larga_y_única
JWT_SECRET=mínimo_16_caracteres_aleatorios

# Creá una base Redis (Upstash) desde Vercel → Storage → Marketplace → Redis y vinculá al proyecto.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
